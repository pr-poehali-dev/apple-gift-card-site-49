export interface GiftCard {
  amount: number;
  originalPrice: number;
  discount: number;
  popular: boolean;
}

export interface Feature {
  icon: string;
  title: string;
  description: string;
  color: string;
}
